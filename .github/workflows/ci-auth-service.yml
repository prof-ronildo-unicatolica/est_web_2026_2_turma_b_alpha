name: CI Auth Service

on:
  push:
    paths:
      - 'apps/services/auth-service/**'
      - 'packages/shared/**'
      - 'packages/contracts/**'

  pull_request:
    paths:
      - 'apps/services/auth-service/**'
      - 'packages/shared/**'
      - 'packages/contracts/**'

jobs:
  test:
    name: Auth Service
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Pipeline Test
        run: |
          echo "Auth Service pipeline executada com sucesso."

      - name: Summary
        if: always()
        run: |
          echo "## Auth Service" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Verificação | Resultado |" >> "$GITHUB_STEP_SUMMARY"
          echo "|---|---|" >> "$GITHUB_STEP_SUMMARY"
          echo "| Pipeline acionada | PASS |" >> "$GITHUB_STEP_SUMMARY"