name: CD Prod

on:
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy Prod
    runs-on: ubuntu-latest

    steps:
      - name: Deployment Test
        run: |
          echo "CD Prod executado com sucesso."

      - name: Summary
        if: always()
        run: |
          echo "## CD Prod" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Verificação | Resultado |" >> "$GITHUB_STEP_SUMMARY"
          echo "|---|---|" >> "$GITHUB_STEP_SUMMARY"
          echo "| Deployment | PASS |" >> "$GITHUB_STEP_SUMMARY"